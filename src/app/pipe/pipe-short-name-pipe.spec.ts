import { PipeShortNamePipe } from './pipe-short-name-pipe';

describe('PipeShortNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeShortNamePipe();
    expect(pipe).toBeTruthy();
  });
});
